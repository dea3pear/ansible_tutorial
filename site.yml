---

- hosts: all
  become: true
  tasks:

  - name: update repository index
    package:
      update_cache: yes

  - name: install apache2 and php packages
    package:
      name: 
        - apache2
        - libapache2-mod-php
      state: latest

